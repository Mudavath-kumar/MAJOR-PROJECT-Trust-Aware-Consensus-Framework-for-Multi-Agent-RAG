# TrustRAG — Trust-Aware Consensus Framework for Multi-Agent RAG

<div align="center">

![TrustRAG Banner](https://img.shields.io/badge/TrustRAG-Enterprise_AI_Platform-1d4ed8?style=for-the-badge&logo=shield&logoColor=white)

[![Frontend Build](https://img.shields.io/badge/Frontend-TanStack_Start_%7C_React_19-blue.svg?style=flat-square)](https://tanstack.com/start)
[![Backend](https://img.shields.io/badge/Backend-Express_%7C_TypeScript-green.svg?style=flat-square)](https://expressjs.com/)
[![AI Engine](https://img.shields.io/badge/AI_Engine-FastAPI_%7C_Python_3.11-teal.svg?style=flat-square)](https://fastapi.tiangolo.com/)
[![Vector Store](https://img.shields.io/badge/VectorStore-ChromaDB-purple.svg?style=flat-square)](https://www.trychroma.com/)
[![Auth](https://img.shields.io/badge/Authentication-Clerk-6C47FF.svg?style=flat-square)](https://clerk.com/)
[![Deployment](https://img.shields.io/badge/Deploy-Render_Blueprint-46E3B7.svg?style=flat-square)](https://render.com)
[![License](https://img.shields.io/badge/License-MIT-amber.svg?style=flat-square)](LICENSE)

**An evidence-first, multi-agent Retrieval-Augmented Generation (RAG) platform with real-time consensus reasoning, confidence scoring, hallucination auditing, and enterprise-grade multi-tenant security.**

[Explore Features](#-key-features) • [Architecture](#-architecture) • [Quick Start](#-quick-start) • [API Reference](#-api-reference) • [Render Blueprint](#-one-click-render-deployment)

---

</div>

## 📌 Executive Summary

Traditional RAG systems suffer from black-box responses, silent hallucinations, and lack of verifiability. **TrustRAG** solves this by decomposing query synthesis across **5 concurrent specialized AI agents** that cross-examine evidence, compute statistical consensus, and assign verifiable trust metrics before presenting answers to the user.

Every answer generated by TrustRAG carries:
1. **Mathematical Trust Score (0–100%)**: Weighted synthesis of source relevance and fact validation.
2. **Consensus Agreement Rate**: Quantitative quorum across independent agent conclusions.
3. **Comprehensive Evaluation Matrix**: Faithfulness, context precision, answer relevance, and hallucination risk.
4. **Verifiable Audit Trail**: Source attribution down to exact document chunks, pages, and token offsets.

---

## ✨ Key Features

### 🤖 5-Agent Consensus Pipeline
- **Agent 1: Researcher (Retriever)** — Queries vector embeddings with hybrid semantic search and BM25 filtering.
- **Agent 2: Fact-Checker** — Cross-references retrieved chunks against user assertions to identify conflicting evidence.
- **Agent 3: Critic** — Stress-tests arguments for logical fallacies, contradictory assertions, and speculative leaps.
- **Agent 4: Trust Assessor** — Measures token-level attribution confidence and computes quantitative hallucination risk.
- **Agent 5: Reasoner** — Synthesizes the final consensus response with weighted evidence citations.

### 📊 Real-Time Evaluation Matrix
- **Faithfulness Score**: Measures whether the generated answer is strictly grounded in retrieved context.
- **Context Precision**: Evaluates the signal-to-noise ratio in retrieved text chunks.
- **Answer Relevance**: Assesses how directly the response satisfies the user's intent.
- **Hallucination Risk Level**: Immediate alert indicator (`LOW`, `MEDIUM`, `HIGH`) flagging ungrounded claims.
- **Consensus Breakdown**: Interactive visualization of agent agreement metrics and dissenting opinions.

### 📁 Multi-File Document Ingestion
- Upload and parse multiple files simultaneously: **PDF, DOCX, TXT, Markdown, CSV** (up to 10 files × 25 MB).
- Intelligent document chunking with metadata tagging, sentence boundary preservation, and token length controls.
- Automatic vectorization via high-performance open embedding models (`BAAI/bge-small-en-v1.5`).

### 🛡️ Enterprise Security & Data Isolation
- **Tenant Isolation**: Every vector collection in ChromaDB and document in MongoDB is strictly partitioned by `user_id`.
- **Clerk Authentication**: Seamless Email/Password and Google OAuth with automatic session token verification.
- **Audit Logs & Compliance**: Export query histories and evaluation matrices as compliance-ready JSON or CSV reports.
- **Content Security**: File type validation, size quotas, sanitize pipelines, and granular error masking.

---

## 🏗️ Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           TRUST-RAG CLIENT                                  │
│         TanStack Start (SSR) + React 19 + Tailwind CSS + Lucide Icons       │
│               Port: 3000 (Local) | Render Web Service (Cloud)               │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │ HTTPS / REST (Bearer JWT)
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          API GATEWAY & BACKEND                              │
│         Node.js + Express + TypeScript + Mongoose (MongoDB Atlas)           │
│     • Clerk JWT Verification  • Rate Limiting  • Multi-File Storage         │
│               Port: 3001 (Local) | Render Web Service (Cloud)               │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │ HTTP Internal RPC (Private Network)
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          AI & CONSENSUS ENGINE                              │
│              Python 3.11 + FastAPI + ChromaDB + HuggingFace                 │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                     5-Agent Consensus Mesh                            │  │
│  │  [Researcher] ──► [Fact-Checker] ──► [Critic] ──► [Trust Assessor]     │  │
│  │                                      │                                │  │
│  │                                      ▼                                │  │
│  │                         [Consensus Synthesis Engine]                  │  │
│  │                                      ▼                                │  │
│  │                            [Final Reasoner]                           │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                      │                                      │
│               Port: 8000 (Local) | Render Web Service (Cloud)               │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🚀 Quick Start

### Prerequisites
- **Node.js 20+** and **Bun** (or npm/pnpm)
- **Python 3.11+** with `pip`
- **MongoDB** (Local instance or free [MongoDB Atlas](https://www.mongodb.com/atlas) cluster)
- **Clerk Account** (Free tier at [clerk.com](https://clerk.com))
- AI Provider API Key: **Google Gemini**, **Groq**, or **OpenRouter**

### 1. Repository Setup
```bash
git clone https://github.com/Mudavath-kumar/MAJOR-PROJECT-Trust-Aware-Consensus-Framework-for-Multi-Agent-RAG.git trustrag
cd trustrag
```

### 2. Environment Configuration

Create the respective `.env` files for each tier:

#### **Frontend (`.env.local` in project root)**
```env
VITE_API_URL=http://localhost:3001/api/v1
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_publishable_key
```

#### **Backend (`backend/.env`)**
```env
NODE_ENV=development
PORT=3001
MONGODB_URI=mongodb://localhost:27017/trustrag
JWT_SECRET=your-secure-jwt-secret-string
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key
CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_publishable_key
AI_SERVICE_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000
```

#### **AI Service (`ai-service/.env`)**
```env
GOOGLE_API_KEY=your_gemini_api_key
GROQ_API_KEY=your_groq_api_key
OPENROUTER_API_KEY=your_openrouter_api_key
CHROMA_DB_PATH=./chroma_db
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
```

---

### 3. Launch Services Locally

You can run all three services concurrently in separate terminal tabs:

#### **Terminal 1: AI Service (FastAPI)**
```bash
cd ai-service
python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

#### **Terminal 2: Backend API (Express)**
```bash
cd backend
npm install
npm run dev
```

#### **Terminal 3: Frontend (TanStack Start)**
```bash
# In project root:
bun install
bun run dev
```

Open [http://localhost:3000](http://localhost:3000) to access the TrustRAG workspace.

---

## 📡 API Reference

### Document Operations
| Method | Endpoint | Description | Auth Required |
|---|---|---|---|
| `POST` | `/api/v1/documents/upload` | Multi-file upload (multipart/form-data) | Yes (Bearer) |
| `GET` | `/api/v1/documents` | List user's indexed documents | Yes (Bearer) |
| `DELETE` | `/api/v1/documents/:id` | Delete document and vector index | Yes (Bearer) |

### Chat & Consensus
| Method | Endpoint | Description | Auth Required |
|---|---|---|---|
| `POST` | `/api/v1/chat/query` | Submit question to 5-agent consensus pipeline | Yes (Bearer) |
| `GET` | `/api/v1/chat/history` | Retrieve conversation history | Yes (Bearer) |
| `GET` | `/api/v1/chat/evaluations/:id`| Retrieve full evaluation matrix & confidence audit | Yes (Bearer) |

### Analytics & Audit
| Method | Endpoint | Description | Auth Required |
|---|---|---|---|
| `GET` | `/api/v1/analytics/overview` | Trust scores, query count, and token usage | Yes (Bearer) |
| `GET` | `/api/v1/analytics/export` | Export compliance report (JSON/CSV) | Yes (Bearer) |

---

## ☁️ One-Click Render Deployment

The repository includes a production-ready `render.yaml` blueprint defining all 3 microservices with automated health checks, environment linking, and build pipelines:

1. Fork or push this repository to GitHub.
2. Sign in to [Render Dashboard](https://dashboard.render.com).
3. Click **New +** ➡️ **Blueprint**.
4. Connect this repository — Render will automatically read `render.yaml` and provision:
   - `trustrag-frontend` (TanStack Start Web Service)
   - `trustrag-backend` (Node.js API Service)
   - `trustrag-ai` (FastAPI Python Service with Dockerfile)
5. Fill in your environment variables (`CLERK_SECRET_KEY`, `GOOGLE_API_KEY`, `MONGODB_URI`).
6. Deploy!

---

## 📂 Project Structure

```
trustrag/
├── ai-service/                  # Python FastAPI AI & RAG Microservice
│   ├── app/
│   │   ├── agents/              # 5-Agent implementations (Researcher, Critic, etc.)
│   │   ├── consensus/           # Consensus synthesis algorithm
│   │   ├── core/                # LLM router & provider fallbacks
│   │   └── rag/                 # Chunker, vectorstore & embeddings
│   ├── requirements.txt         # Python dependencies
│   └── Dockerfile.ai-service    # Production container spec
├── backend/                     # Node.js + Express REST API
│   ├── src/
│   │   ├── config/              # MongoDB & environment config
│   │   ├── controllers/         # Request handlers (Chat, Docs, Analytics)
│   │   ├── middleware/          # Clerk auth, error handler, rate limits
│   │   ├── models/              # Mongoose schemas (Messages, Audits, Users)
│   │   └── services/            # Internal RPC & storage services
│   └── package.json
├── src/                         # TanStack Start SSR Frontend
│   ├── components/              # UI components, AuthPage, Evaluation Modals
│   ├── routes/                  # File-based routing (Chat, Upload, Analytics)
│   ├── lib/                     # API client, state stores, utilities
│   └── styles.css               # Design system & CSS custom properties
├── public/                      # Static assets & standalone landing page
├── render.yaml                  # Render Blueprint deployment manifest
└── README.md                    # Project documentation
```

---

## 📜 License

This project is licensed under the [MIT License](LICENSE) — feel free to use, modify, and distribute for educational or commercial applications.
